# This is an example .env file. Copy it to .env and adjust the values.

# The source for population data.
# Available options: "wiki", "statstimes"
POPULATION_SOURCE=wiki

# The DATABASE_URL is set automatically by docker-compose for the services.
# You only need to set it in your local .env if you run scripts outside of Docker.
#
# For local execution against the Docker DB:
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/population
#
# For execution inside Docker services (this is the default in docker-compose.yml):
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/population
